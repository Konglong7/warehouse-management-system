# Claude 行为规范

## 执行规则
- 所有命令必须拆分为简单步骤，禁止使用 &&、||、重定向等复杂组合命令
- 对于安全命令（如 cd、ls、读取文件），直接执行，不需要确认
- 只有涉及删除、覆盖、安装依赖时才询问用户

## 输出规则
- 所有回答使用中文
- 关键英文提示需要附带中文解释

## 风格要求
- 优先保证命令清晰，而不是一行复杂写法
- 尽量减少用户交互（yes/no）
